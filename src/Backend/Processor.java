package Backend;

public class Processor {

    public static String sortString(String inputString, Integer mode) {
        String returnString;
                     
        // Allow different modes to test different sorting algorithms
        if (mode == 1) {
            returnString = "Notimplemented yet";            
        } else {
            returnString = "Invalid mode";
        }            

        return returnString;
    }
    
}